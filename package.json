{
  "name": "@bounded-systems/desk",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "The Front Desk, live — a standalone Worker that renders the org's ranked board at request time.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "icons": "node scripts/make-icons.mjs",
    "tokens": "node scripts/make-tokens.mjs"
  },
  "devDependencies": {
    "wrangler": "^4",
    "@bounded-systems/brand": "^2.0.0"
  }
}
